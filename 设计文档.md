# 三打一扑克游戏设计文档

## 1. 架构目标

本项目实现标准四人三打一扑克游戏，采用前后端分离思路：

- 后端使用 Python + Flask 提供 HTTP API、静态资源和 WebSocket 服务。
- Flask 与 WebSocket 复用同一端口，默认端口为 `5000`。
- 游戏规则被抽象到 `sandayi.model`，不依赖 Flask、WebSocket 或数据库。
- 服务层只负责房间管理、玩家连接、消息路由和广播。
- 所有并发处理使用 Flask threaded 模式、线程锁和 AI 玩家线程，不使用异步框架。
- AI 玩家以 Python WebSocket 客户端身份加入游戏，服务端对人类玩家和 AI 玩家等同处理。

## 2. 目录结构

```text
sandayi/
  model/          独立游戏规则模型
  server/         Flask 路由、房间管理、WebSocket 通信
  ai/             AI WebSocket 客户端
static/           前端静态页面
requirements.txt 依赖清单
```

## 3. 核心模块

### 3.1 规则模型

- `sandayi/model/cards.py`
  - 定义 `Card`、`Suit`、整副牌生成、分牌、主牌判定和排序。
- `sandayi/model/game.py`
  - 定义游戏阶段、玩家状态、叫牌记录、轮次记录和 `SandayiGame`。
  - 负责发牌、叫牌、扣底、定主、出牌、跟牌校验、轮次结算和最终积分结算。

规则模型仅暴露方法调用，不感知网络协议：

- `add_player`
- `start`
- `place_bid`
- `bury_kitty`
- `choose_trump`
- `play_card`
- `public_view`
- `legal_actions`

### 3.2 服务层

- `sandayi/server/room_manager.py`
  - 管理房间集合。
  - 每个房间持有一个 `SandayiGame` 和一个重入锁。
  - 收到动作后在房间锁内调用规则模型，再广播最新状态。
- `sandayi/server/app.py`
  - 提供 HTTP API：创建房间、加入房间、查看房间、补齐 AI。
  - 提供 WebSocket：`/ws/<room_id>/<player_id>`。
  - 托管 `static` 目录作为临时前端资源。

### 3.3 AI 客户端

- `sandayi/ai/client.py`
  - `AIPlayer` 是 Python 类，不通过命令行启动脚本。
  - Flask 后端点击补齐 AI 后，直接实例化 `AIPlayer`。
  - AI 使用 HTTP 加入房间，再使用 WebSocket 接入游戏。
  - AI 决策以正确性为主：
    - 只从服务端返回的合法动作中选择。
    - 手牌一般时放弃叫牌，较好时最低合法叫分。
    - 扣底优先扣低价值非分牌。
    - 定主选择手牌最多的花色。
    - 出牌选择合法牌中最小牌。

## 4. 时序设计

### 4.1 创建与加入房间

1. 前端调用 `POST /api/rooms` 创建房间。
2. 前端调用 `POST /api/rooms/<room_id>/join` 加入房间。
3. 前端连接 `/ws/<room_id>/<player_id>`。
4. 服务端推送玩家视角的 `state`。

### 4.2 补齐 AI

1. 房间未满 4 人时，前端调用 `POST /api/rooms/<room_id>/ai`。
2. 服务端计算缺少人数。
3. 每缺少一名玩家，服务端新建一个 `AIPlayer` 类实例。
4. AI 线程调用加入房间 API。
5. AI 连接 WebSocket，和普通玩家一致接收状态、发送动作。

### 4.3 游戏主流程

1. 满 4 人后，玩家发送 `{ "type": "start" }`。
2. 规则模型洗牌并发牌，每人 12 张，底牌 6 张。
3. 进入叫牌阶段，首叫人为持有方块 3 的玩家。
4. 叫牌结束后，庄家拿底牌并进入扣底阶段。
5. 庄家扣 6 张牌，然后定主。
6. 进入出牌阶段，首轮庄家先出。
7. 每轮所有玩家出一张牌，模型严格校验跟牌规则。
8. 轮次赢家收取分牌，并成为下一轮首家。
9. 所有手牌出完后，结算胜负、抠底、清光、平分和积分。

## 5. 并发设计

- Flask 使用 `threaded=True` 运行。
- 每个 WebSocket 连接由服务端线程处理。
- 每个房间有独立 `RLock`，防止多个玩家同时操作造成状态竞争。
- AI 玩家每个实例一个后台线程。
- 广播在房间锁内基于当前状态生成各玩家私有视图。

## 6. 规则校验要点

- 人数必须为 4 才能开始。
- 叫分只能为 60、65、70、75、80。
- 叫分必须高于当前最高叫分。
- 起叫限制按前置放弃人数确定：前两位可 60，第三位最低 65，第四位最低 70。
- 庄家必须扣 6 张存在于自己手牌中的牌。
- 主牌只能选择四种普通花色。
- 出牌必须轮到当前玩家。
- 出牌必须存在于手牌。
- 跟牌时若有首家有效花色牌，必须跟同有效花色。
- 当前标准规则只支持单张出牌。

## 7. 启动方式

先进入 conda 环境：

```bash
conda activate sandayi
pip install -r requirements.txt
python -m sandayi.server.app
```

浏览器打开：

```text
http://127.0.0.1:5000
```

## 8. 当前阶段限制

- 暂未持久化房间状态，服务重启后房间消失。
- AI 只保证动作合法，不追求胜率最优。
- 逃跑处理和亮牌跳过已在规则文档中识别，但当前版本未接入前端按钮。
- 全放弃采用“常主数量最多者获胜”的标准平台处理。
